apiVersion: v1
kind: Service
metadata:
  name: aspecta-demo-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aspecta-demo
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
